# World creator
from graphics import *
import config

# Main function
def main():
    # Initialize window
    window = GraphWin("Pacman world editor", config.WINDOW_WIDTH, config.WINDOW_HEIGHT)

    # Main loop
    while True:

        break # Completing this later

    # Save data
    path = input("Name of world: ")
    
